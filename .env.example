# Database
DATABASE_URL="postgresql://tplogistics_user:CHANGE_ME@localhost:5432/tplogistics?schema=public"

# NextAuth
NEXTAUTH_URL="https://app.tplogist.com"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Node Environment
NODE_ENV="development"

# API keys
TRACKING_API_KEY="generate-with-openssl-rand-hex-24"

# SMTP (Brevo) — boletin emails
SMTP_HOST="smtp-relay.brevo.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="TP Logistics <import@tplogist.com>"
